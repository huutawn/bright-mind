version: '3.8'

services:
  db:
    image: postgres:16
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgresql.conf:/etc/postgresql/postgresql.conf
    environment:
      # ✅ Tên database là "fast"
      - POSTGRES_DB=fast
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5433:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:RELEASE.2025-03-12T18-04-18Z
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio_data:/data
    restart: unless-stopped
    healthcheck:
        test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
        interval: 30s
        timeout: 20s
        retries: 3

#  # ✅ Bỏ comment và sửa lại tên DB
#  alembic:
#    build: . # Xây dựng từ Dockerfile trong thư mục hiện tại
#    image: fastapi-base:latest
#    environment:
#      # Sửa tên DB thành "fast" để khớp với service db
#      SQL_DATABASE_URL: 'postgresql+psycopg2://postgres:postgres@db:5432/fast'
#      SECRET_KEY: ${SECRET_KEY}
#    command: [ "alembic", "upgrade", "head" ]
#    depends_on:
#      db:
#        condition: service_healthy
#
#  # ✅ Thêm service cho ứng dụng FastAPI của bạn
#  app:
#    build: . # Cũng xây dựng từ Dockerfile
#    image: fastapi-base:latest
#    environment:
#      # Sửa tên DB thành "fast" và dùng driver asyncpg
#      DATABASE_URL: 'postgresql+asyncpg://postgres:postgres@db:5432/fast'
#      SECRET_KEY: ${SECRET_KEY}
#    ports:
#      - "8000:8000"
#    depends_on:
#      db:
#        condition: service_healthy
#      minio:
#        condition: service_healthy

volumes:
  postgres_data:
  minio_data: